// Re-exports the shared type-colour palette (lib/typeColors.js) so mobile
// screens keep their existing "../lib/typeColors" import path.
export { TYPE_COLORS, typeColor } from "../../lib/typeColors";
